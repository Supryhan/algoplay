package catslaboratory.problems

object FactorialTrampoline extends App {

//  private def fact(recur: (=> Int) => Int)(n: Int): Int = {
//    if (n == 0) 1
//    else n * recur(n - 1)
//  }
//
//  private def fix[A, B](f: (A => B) => (A => B)): (A => B) =
//    f(fix(f)(_))
//
//  private val factFixed: Int => Int = fix(fact)
//
//  println(s"Factorial of 0 = ${factFixed(0)}")
//  println(s"Factorial of 5 = ${factFixed(5)}")

}
